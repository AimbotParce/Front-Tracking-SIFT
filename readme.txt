To run the program, run main_video_tracking.py on a terminal, the UI will automatically open and the program will start loading the video.

To change wich video to play, change the file_path variable on the main_video_tracking.py program.